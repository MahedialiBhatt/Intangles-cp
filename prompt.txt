Read and use the following files as mandatory implementation context before starting any work:

* INSTRUCTIONS.md
* intangles-onprem-master-plan.md
* intangles-implementation-reference.md

Operate in FULL autonomous development mode.

Analyze the complete repository structure first and identify:

* existing implementations
* completed phases
* missing phases
* broken architecture areas
* technical debt
* missing tests
* missing documentation
* migration risks
* operational gaps

Then generate an internal phased execution plan and begin implementation incrementally across:

* intangles-operator
* intangles-cp-server
* intangles-cp-dashboard

Requirements:

* preserve backward compatibility
* preserve APP_TYPE compatibility unless explicitly migrated
* preserve current onboarding/deployment flow
* preserve Kubernetes-native architecture
* extend existing systems instead of rewriting stable implementations
* implement foundational phases before advanced features
* automatically create/update tests
* automatically create/update README.md
* automatically create/update CLAUDE.md
* automatically update architecture/operational docs
* automatically refactor where operationally justified
* automatically add migrations where required
* automatically validate compatibility after changes
* commit changes incrementally as phases/features complete

Development lifecycle for every feature:
Plan → Write → Test → Validate → Document → Refactor → Commit

Testing requirements:

* run unit tests
* run integration tests
* run e2e tests for critical flows where possible
* validate operator builds
* validate frontend builds
* validate API contracts
* validate migration safety
* validate rollback safety

Environment/tooling behavior:

* install missing dependencies/tools only if required for development/testing
* track all temporary installations performed during execution
* after validation/testing completes, remove temporary tools/dependencies that were not originally present on the machine
* clean temporary files, containers, caches, test artifacts, and local runtime dependencies created during execution
* do not leave unnecessary local tooling/installations behind
* preserve only repository-required dependencies and artifacts

Operational priorities:

1. stability
2. correctness
3. backward compatibility
4. observability
5. maintainability
6. performance
7. scalability
8. enterprise features

Required architecture behaviors:

* dependency-aware reconciliation
* semantic health evaluation
* reconcile backpressure protection
* retry with exponential backoff/jitter
* unified component model
* component ownership boundaries
* safe CRD/schema migrations
* capability negotiation support
* operational-first UX
* lightweight frontend architecture
* reusable abstractions
* migration-safe rollout patterns

Avoid:

* unnecessary rewrites
* giant monolithic reconcilers
* frontend overengineering
* heavy UI frameworks
* premature microservice decomposition
* duplicated deployment logic
* hardcoded feature mappings
* uncontrolled polling loops
* breaking API/protocol compatibility
* unnecessary enterprise complexity

Frontend rules:

* Zustand for global state
* React Query for server-state/polling
* reusable UI primitives
* operational UX over marketing UI
* avoid Redux/MaterialUI/microfrontends

Backend/operator rules:

* modular services/controllers
* Kubernetes-native patterns
* idempotent reconciliation
* semantic component health
* safe retries/backoff
* additive migrations preferred

Continue implementation autonomously phase-by-phase until all planned foundational phases are completed, tested, documented, validated, and committed.

